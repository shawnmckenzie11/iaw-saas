# Copy to `.env.test` (gitignored) for optional Playwright overrides.
# Base seed credentials are read from `backend/.env` (see playwright.config.ts).

# Optional E2E overrides (default to SEED_* from backend/.env)
# E2E_DISPATCHER_EMAIL=
# E2E_DISPATCHER_PASSWORD=
# E2E_DRIVER1_PIN=
# E2E_DRIVER2_PIN=
